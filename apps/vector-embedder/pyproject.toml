[project]
name = "vector-embedder"
version = "0.1.0"
description = "Text embedding generator for Elasticsearch vector search"
requires-python = ">=3.11"

dependencies = [
    "sentence-transformers>=2.6.0",
]

[project.scripts]
vector-embedder = "src.cli:main"